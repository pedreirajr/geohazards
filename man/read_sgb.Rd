% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sgb.R
\name{read_sgb}
\alias{read_sgb}
\title{Read a geological risk product from the SGB}
\usage{
read_sgb(product = "risk", municipality = NULL, state = NULL, code_muni = NULL)
}
\arguments{
\item{product}{Product identifier, as listed by \code{\link[=sgb_products]{sgb_products()}}. Defaults
to \code{"risk"} (risk sectorisation).}

\item{municipality}{Municipality name. Matching is case-insensitive and
tolerant to accents, so \code{"Petropolis"} finds "Petrópolis". Combine with
\code{state} when the name occurs in more than one state.}

\item{state}{Two-letter state abbreviation (e.g. \code{"RJ"}).}

\item{code_muni}{IBGE municipality code, as text or number. Takes precedence
over \code{municipality} and is never ambiguous.}
}
\value{
An \code{sf} object in SIRGAS 2000 (EPSG:4674), possibly with zero rows.

Field names are translated to English. The mapping from the original SGB
field names is:
\describe{
\item{common}{\code{municipio}/\code{munic} -> \code{name_muni}, \code{uf} ->
\code{abbrev_state}, \code{cd_geocmu} -> \code{code_muni}}
\item{risk, risk_amazonas}{\code{grau_risco} -> \code{risk_level}, \code{tipolo_g1} ->
\code{process_type}, \code{tipolo_e1} -> \code{process_subtype}, \code{cobrade_01} ->
\code{cobrade}, \code{data_setor} -> \code{survey_date}, \code{num_pess} -> \code{n_people},
\code{num_edif} -> \code{n_buildings}, \code{num_domi} -> \code{n_households}, \code{local} ->
\code{locality}}
\item{flood}{\code{classe} -> \code{flood_class}, \code{processo} -> \code{process}, \code{ano}
-> \code{year}}
\item{occurrence_*}{\code{evento} -> \code{event}, \code{evento_data} -> \code{event_date},
\code{chovia} -> \code{was_raining}, \code{qtd_imoveis_atingidos} ->
\code{n_affected_buildings}, \code{observacoes} -> \code{notes}, \code{analisado} ->
\code{reviewed}}
}
Fields absent from this mapping are returned under their original names.
Date fields, which the server encodes as epoch milliseconds, are returned
as \code{Date}.

Attribute \emph{values} remain as published by the SGB, in Portuguese: for
example \code{risk_level} takes \code{"Alto"} and \code{"Muito alto"}. \code{n_people},
\code{n_buildings} and \code{n_households} are the SGB's own per-sector estimates.
The occurrence layers include an \code{email} field from the original report,
which is personal data and should not be redistributed.
}
\description{
Downloads one of the geological risk products published by the Geological
Survey of Brazil (SGB/CPRM) as an \code{sf} object. See \code{\link[=sgb_products]{sgb_products()}} for the
catalogue of products and \code{\link[=sgb_inventory]{sgb_inventory()}} for what exists in a given
municipality or state before downloading it.

The area is selected by municipality, either by name (\code{municipality}, best
combined with \code{state} to disambiguate) or by IBGE code (\code{code_muni}). Giving
neither downloads the whole \code{state}, and giving neither \code{state} nor a
municipality downloads the whole country, which can be large.
}
\details{
The SGB mapping is not universal. A municipality with no features returns an
\code{sf} object with zero rows and a warning: absence of mapping is not absence
of risk.

The national occurrence layers (\code{"occurrence_mobile"},
\code{"occurrence_pending"}) carry no municipality attribute. Asking for one of
them by municipality clips them spatially against the official IBGE
boundary, which requires the \pkg{geobr} package.
}
\examples{
\dontrun{
  # Flood mapping for a municipality
  read_sgb("flood", municipality = "Nova Viçosa", state = "BA")

  # By IBGE code, which never needs disambiguation
  read_sgb("risk", code_muni = 3300100)

  # Every mapped risk sector in a state
  read_sgb("risk", state = "RJ")
}

}
\seealso{
\code{\link[=sgb_products]{sgb_products()}}, \code{\link[=sgb_inventory]{sgb_inventory()}}, \code{\link[=read_sgb_risk]{read_sgb_risk()}}
}
