% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sgb-inventory.R
\name{sgb_inventory}
\alias{sgb_inventory}
\title{Inventory of SGB products}
\usage{
sgb_inventory(
  municipality = NULL,
  state = NULL,
  code_muni = NULL,
  by = c("municipality", "state", "country"),
  products = NULL,
  complete = FALSE
)
}
\arguments{
\item{municipality}{Municipality name, or a character vector of names, when
\code{by = "municipality"}.}

\item{state}{Two-letter state abbreviation (e.g. \code{"RJ"}).}

\item{code_muni}{IBGE municipality code, as text or number. Takes precedence
over \code{municipality} and is never ambiguous.}

\item{by}{Level of detail: \code{"municipality"} (default), \code{"state"} or
\code{"country"}.}

\item{products}{Optional character vector restricting the products that are
inventoried. Defaults to every queryable product.}

\item{complete}{When \code{by = "municipality"}, whether to include products with
zero features. Defaults to \code{FALSE}.}
}
\value{
A \code{data.frame}, whose columns depend on \code{by}:
\describe{
\item{\code{"municipality"}}{\code{name_muni}, \code{abbrev_state}, \code{theme}, \code{product},
\code{type}, \code{n_features}.}
\item{\code{"state"}}{\code{product}, \code{theme}, \code{abbrev_state}, \code{name_muni},
\code{code_muni}.}
\item{\code{"country"}}{\code{code_muni}, \code{name_muni}, \code{abbrev_state}, one logical
column per product, and \code{n_products}.}
}
}
\description{
Reports what the Geological Survey of Brazil has mapped, before any geometry
is downloaded. Three levels of detail are available through \code{by}:
\itemize{
\item \code{"municipality"} — which products exist for one or more municipalities,
with the number of features of each.
\item \code{"state"} — which municipalities of a state (or of the whole country, if
\code{state} is \code{NULL}) have each product.
\item \code{"country"} — one row per municipality in the country, with one logical
column per product, for a coverage overview.
}
}
\details{
Only the queryable layers are inventoried; see \code{\link[=sgb_products]{sgb_products()}}. The
national occurrence layers carry no municipality attribute and therefore
never appear in the inventory, even though they can be downloaded with
\code{\link[=read_sgb]{read_sgb()}}.
}
\examples{
\dontrun{
  # What exists for one municipality
  sgb_inventory("Angra dos Reis", state = "RJ")

  # ... and for several at once
  sgb_inventory(c("Angra dos Reis", "Petrópolis"), state = "RJ")

  # Every municipality of Bahia with mapping
  sgb_inventory(state = "BA", by = "state")

  # National coverage: municipalities with both risk and flood mapping
  coverage <- sgb_inventory(by = "country")
  subset(coverage, risk & flood)
}

}
\seealso{
\code{\link[=sgb_products]{sgb_products()}}, \code{\link[=read_sgb]{read_sgb()}}
}
