% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sgb-rigeo.R
\name{read_sgb_report}
\alias{read_sgb_report}
\title{Read a technical report from the SGB repository (RIGeo)}
\usage{
read_sgb_report(handle, dir = NULL)
}
\arguments{
\item{handle}{RIGeo handle of the item, such as \code{"doc/17701"}. The full URL
(\code{"https://rigeo.sgb.gov.br/handle/doc/17701"}) is also accepted.}

\item{dir}{Directory to extract into. Defaults to a subdirectory of the
session's temporary directory.}
}
\value{
A list with:
\describe{
\item{item}{The item's \code{uuid}, \code{title} and \code{handle}.}
\item{bitstreams}{A \code{data.frame} of every file attached to the item,
with \code{name}, \code{uuid}, \code{bytes} and download \code{url}.}
\item{files}{Paths of the files extracted into \code{dir}.}
}
}
\description{
Downloads and extracts the package deposited for a RIGeo item: the technical
report itself, usually as PDF, and, when present, the shapefiles that
accompany it. RIGeo is the institutional repository of the Geological Survey
of Brazil, and the risk sectorisation of a municipality is normally
documented there.
}
\section{Dependencies}{

Requires the \pkg{zip} package, which is a suggested dependency.
}

\examples{
\dontrun{
  report <- read_sgb_report("doc/17701")
  report$item$title
  report$files
}

}
