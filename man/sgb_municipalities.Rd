% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sgb-municipalities.R
\name{sgb_municipalities}
\alias{sgb_municipalities}
\title{List Brazilian municipalities known to the SGB}
\usage{
sgb_municipalities(search = NULL, state = NULL, source = c("local", "server"))
}
\arguments{
\item{search}{Optional fragment of a municipality name. Matching is
case-insensitive and tolerant to accents, so \code{"petropolis"} finds
"Petrópolis".}

\item{state}{Optional two-letter state abbreviation (e.g. \code{"RJ"}).}

\item{source}{\code{"local"} to search the embedded IBGE table, or \code{"server"} to
list the municipalities covered by the SGB risk sectorisation.}
}
\value{
A \code{data.frame} with \code{code_muni}, \code{name_muni} and \code{abbrev_state}.
}
\description{
Two catalogues in one function. With \code{source = "local"} (the default) it
searches the IBGE municipality table embedded in the package, which works
offline and is the way to find the \code{code_muni} of a municipality. With
\code{source = "server"} it asks the SGB which municipalities actually have risk
sectorisation mapped, which requires network access.
}
\examples{
# Offline search of the embedded IBGE table
sgb_municipalities(search = "petropolis")

\dontrun{
  # Municipalities with risk sectorisation mapped by the SGB, in Bahia
  sgb_municipalities(state = "BA", source = "server")
}

}
